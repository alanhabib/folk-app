import React from 'react';
import "./Header.css"

const header = (props) => {
    return (
        <header className="header">
            <p>Folke-appen</p>
        </header>
    );
}

export default header;
